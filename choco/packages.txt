# Chocolatey fallback packages
# These are packages not available in WinGet (or where choco is preferred).
# Primary package manager is WinGet — see ../winget/packages.json.

# .NET Framework legacy runtimes (no exact match in WinGet)
dotnet4.7.1
dotnetfx

# Process analysis (ProcessHacker is discontinued; successor is SystemInformer)
# Available in WinGet as WinsiderSS.SystemInformer — kept here as choco fallback only
processhacker

# claude-code: this is the CLI tool, installed via npm/bun (not WinGet).
# See setup_windows.ps1 post-install section.
